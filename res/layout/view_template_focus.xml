<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/focus_icon"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/focus_title"
        android:layout_width="fill_parent"
        android:layout_height="32dip"
        android:layout_alignBottom="@id/focus_icon"
        android:layout_alignLeft="@id/focus_icon"
        android:layout_alignRight="@id/focus_icon"
        android:background="@drawable/background_text_translucence"
        android:gravity="center_vertical|left"
        android:paddingLeft="8dip"
        android:singleLine="true"
        android:text="@string/app_name"
        android:textColor="@color/textcolor_channel_template_item_white"
        android:textSize="@dimen/channel_template_item_title_textsize" />

</RelativeLayout>
